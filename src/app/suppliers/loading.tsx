import { RouteLoadingState } from "@/components/route-state";

export default function Loading() {
  return <RouteLoadingState title="Cargando proveedores" description="Estamos preparando la cartera de proveedores." />;
}
