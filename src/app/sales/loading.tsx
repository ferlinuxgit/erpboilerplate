import { RouteLoadingState } from "@/components/route-state";

export default function Loading() {
  return <RouteLoadingState title="Cargando ventas" description="Estamos preparando el flujo comercial y sus acciones pendientes." />;
}
