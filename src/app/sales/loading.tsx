import { RouteLoadingState } from "@/components/route-state";

export default function Loading() {
  return <RouteLoadingState title="Cargando documentos comerciales" description="Estamos preparando el listado, sus filtros y acciones disponibles." />;
}
