import { RouteLoadingState } from "@/components/route-state";

export default function Loading() {
  return <RouteLoadingState title="Cargando facturas" description="Estamos preparando el formulario y listado de facturas." />;
}
