import { and, eq } from "drizzle-orm"; import { notFound } from "next/navigation";
import { ItemForm } from "@/components/inventory/item-form"; import { PageHeader, PageSection, PageShell } from "@/components/ui/page"; import { item } from "@/db/schema"; import { requireContext } from "@/lib/current-context"; import { db } from "@/lib/db";
export default async function EditItemPage({ params }: { params: Promise<{ id: string }> }) { const ctx = await requireContext("stock.write"); const { id } = await params; const [record] = await db.select().from(item).where(and(eq(item.id, id), eq(item.companyId, ctx.company.id))).limit(1); if (!record) notFound(); return <PageShell><PageHeader eyebrow="Inventario · Artículos" title={`Editar ${record.name}`} description={record.sku} backHref={`/inventory/items/${record.id}`} backLabel="Volver a la ficha" /><PageSection title="Datos del artículo" description="Actualiza precios y parámetros sin perder su historial."><ItemForm id={record.id} initialValues={{ name: record.name, sku: record.sku, isService: record.isService, salePrice: record.salePrice, costPrice: record.costPrice, minimumStock: record.minimumStock }} /></PageSection></PageShell>; }
