import { and, desc, eq } from "drizzle-orm"; import Link from "next/link"; import { notFound } from "next/navigation";
import { buttonVariants } from "@/components/ui/button"; import { MetricCard, PageHeader, PageSection, PageShell } from "@/components/ui/page"; import { StatusBadge } from "@/components/ui/status-badge"; import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"; import { item, itemCostHistory, stockLocation, stockMovement, warehouse } from "@/db/schema"; import { requireContext } from "@/lib/current-context"; import { db } from "@/lib/db"; import { formatDate, formatMoney } from "@/lib/format"; import { can } from "@/lib/rbac";
export default async function ItemDetailPage({ params }: { params: Promise<{ id: string }> }) { const ctx = await requireContext("stock.read"); const { id } = await params; const [record] = await db.select().from(item).where(and(eq(item.id, id), eq(item.companyId, ctx.company.id))).limit(1); if (!record) notFound(); const [locations, movements, costs] = await Promise.all([db.select({ id: stockLocation.id, warehouseId: warehouse.id, warehouseCode: warehouse.code, warehouseName: warehouse.name, quantity: stockLocation.currentQuantity, averageCost: stockLocation.averageCost }).from(stockLocation).innerJoin(warehouse, eq(warehouse.id, stockLocation.warehouseId)).where(and(eq(stockLocation.companyId, ctx.company.id), eq(stockLocation.itemId, id))), db.select({ id: stockMovement.id, movementType: stockMovement.movementType, quantity: stockMovement.quantity, movedAt: stockMovement.movedAt, reason: stockMovement.reason, reference: stockMovement.reference, warehouseName: warehouse.name }).from(stockMovement).innerJoin(warehouse, eq(warehouse.id, stockMovement.warehouseId)).where(and(eq(stockMovement.companyId, ctx.company.id), eq(stockMovement.itemId, id))).orderBy(desc(stockMovement.movedAt)).limit(20), db.select().from(itemCostHistory).where(and(eq(itemCostHistory.companyId, ctx.company.id), eq(itemCostHistory.itemId, id))).orderBy(desc(itemCostHistory.createdAt)).limit(10)]); const quantity = locations.reduce((total, row) => total + Number(row.quantity), 0); const inventoryValue = locations.reduce((total, row) => total + Number(row.quantity) * Number(row.averageCost), 0); const currency = ctx.company.baseCurrencyCode; return <PageShell><PageHeader eyebrow="Inventario · Artículo" title={record.name} description={record.sku} backHref="/inventory/items" backLabel="Volver a artículos" meta={<StatusBadge tone={record.isService ? "info" : quantity <= Number(record.minimumStock) ? "warning" : "success"}>{record.isService ? "Servicio" : quantity <= Number(record.minimumStock) ? "Stock bajo" : "Disponible"}</StatusBadge>} actions={<>{can(ctx.membership.role, "stock.write") ? <Link className={buttonVariants({ variant: "outline" })} href={`/inventory/items/${record.id}/edit`}>Editar</Link> : null}{!record.isService ? <Link className={buttonVariants()} href={`/inventory/movements/new?itemId=${record.id}`}>Nuevo movimiento</Link> : null}</>} /><section className="grid gap-3 md:grid-cols-4"><MetricCard label="Stock total" value={record.isService ? "—" : quantity.toLocaleString("es-ES", { maximumFractionDigits: 3 })} helper={`Mínimo ${Number(record.minimumStock).toLocaleString("es-ES")}`} tone={!record.isService && quantity <= Number(record.minimumStock) ? "warning" : "neutral"} /><MetricCard label="Valor de stock" value={formatMoney(inventoryValue, currency)} helper="Cantidad × coste medio" /><MetricCard label="Precio de venta" value={formatMoney(record.salePrice, currency)} helper="Precio por defecto" /><MetricCard label="Coste medio" value={formatMoney(record.averageCost, currency)} helper={`${costs.length} actualizaciones recientes`} /></section><PageSection title="Existencias por almacén" description="Cantidad y valoración en cada ubicación."><div className="overflow-x-auto rounded-xl border"><Table><TableHeader><TableRow><TableHead>Almacén</TableHead><TableHead className="text-right">Cantidad</TableHead><TableHead className="text-right">Coste medio</TableHead><TableHead className="text-right">Valor</TableHead></TableRow></TableHeader><TableBody>{locations.length === 0 ? <TableRow><TableCell className="text-muted-foreground" colSpan={4}>Sin existencias registradas.</TableCell></TableRow> : locations.map((row) => <TableRow key={row.id}><TableCell><Link className="font-medium hover:underline" href={`/inventory/warehouses/${row.warehouseId}`}>{row.warehouseCode} · {row.warehouseName}</Link></TableCell><TableCell className="text-right font-mono">{Number(row.quantity).toLocaleString("es-ES")}</TableCell><TableCell className="text-right">{formatMoney(row.averageCost, currency)}</TableCell><TableCell className="text-right font-mono">{formatMoney(Number(row.quantity) * Number(row.averageCost), currency)}</TableCell></TableRow>)}</TableBody></Table></div></PageSection><PageSection title="Movimientos recientes" description="Trazabilidad de entradas, salidas, ajustes y transferencias." contentClassName="space-y-2">{movements.length === 0 ? <p className="text-sm text-muted-foreground">No hay movimientos.</p> : movements.map((row) => <div className="grid gap-2 rounded-lg border p-3 text-sm sm:grid-cols-[auto_1fr_auto]" key={row.id}><StatusBadge tone={row.movementType === "OUT" ? "warning" : "success"}>{row.movementType}</StatusBadge><div><p className="font-medium">{row.reason}</p><p className="text-xs text-muted-foreground">{row.warehouseName}{row.reference ? ` · ${row.reference}` : ""}</p></div><div className="text-right"><p className="font-mono font-semibold">{Number(row.quantity).toLocaleString("es-ES")}</p><p className="text-xs text-muted-foreground">{formatDate(row.movedAt)}</p></div></div>)}</PageSection></PageShell>; }
