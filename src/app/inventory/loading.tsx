import { RouteLoadingState } from "@/components/route-state";

export default function Loading() {
  return <RouteLoadingState title="Cargando inventario" description="Estamos preparando existencias, movimientos y operaciones." />;
}
