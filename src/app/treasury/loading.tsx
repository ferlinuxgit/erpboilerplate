import { RouteLoadingState } from "@/components/route-state";

export default function Loading() {
  return <RouteLoadingState title="Cargando tesorería" description="Estamos preparando cuentas bancarias, movimientos y conciliación." />;
}
