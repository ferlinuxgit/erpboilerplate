import { RouteNotFoundState } from "@/components/route-state";

export default function NotFound() {
  return <RouteNotFoundState title="Onboarding no encontrado" description="La pantalla de configuración solicitada no está disponible." />;
}
