import { RouteLoadingState } from "@/components/route-state";

export default function Loading() {
  return <RouteLoadingState title="Cargando onboarding" description="Estamos preparando los pasos de configuración inicial." />;
}
