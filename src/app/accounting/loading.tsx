import { RouteLoadingState } from "@/components/route-state";

export default function Loading() {
  return <RouteLoadingState title="Cargando contabilidad" description="Estamos preparando cuentas, asientos y balances." />;
}
