import { RouteLoadingState } from "@/components/route-state";

export default function Loading() {
  return <RouteLoadingState title="Cargando operaciones de compra" description="Estamos preparando documentos, vencimientos y acciones disponibles." />;
}
