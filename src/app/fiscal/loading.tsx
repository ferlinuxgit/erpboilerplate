import { RouteLoadingState } from "@/components/route-state";

export default function Loading() {
  return <RouteLoadingState title="Cargando fiscalidad" description="Estamos preparando modelos, configuración y resumen fiscal." />;
}
