import { RouteLoadingState } from "@/components/route-state";

export default function Loading() {
  return <RouteLoadingState title="Cargando suscripción" description="Estamos preparando plan, límites y acciones de facturación." />;
}
